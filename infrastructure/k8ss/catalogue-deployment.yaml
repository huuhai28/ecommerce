apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogue-service
  namespace: ecommerce
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: catalogue-service
  template:
    metadata:
      labels:
        app: catalogue-service
    spec:
      initContainers:
        - name: wait-for-db
          image: postgres:14
          command: ["sh", "-c", "until pg_isready -h catalogue-db; do sleep 2; done"]
      containers:
        - name: catalogue-service
          image: huuhai123/catalogue-service:latest
          env:
            - name: PORT
              value: "3001"
            - name: DB_HOST
              value: "catalogue-db"
            - name: DB_PORT
              value: "5432"
            - name: DB_DATABASE
              value: "catalogue_db"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: DB_PASSWORD
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          ports: 
            - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: catalogue-service
  namespace: ecommerce
spec:
  type: NodePort
  selector:
    app: catalogue-service
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30004